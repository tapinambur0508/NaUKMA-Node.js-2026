function calculateTotal(price: number, quantity: number) {
  return price * quantity;
}

console.log(calculateTotal(10, 2)); // 20
console.log(calculateTotal("Hello", 1));

function sum(a: number, b: number) {
  return a * b;
}

console.log(sum(2, 5));
