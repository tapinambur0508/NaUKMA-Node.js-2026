# Домашнє завдання 2

## Library Management System (База даних + Auth)

**Курс:** Backend розробка на Node.js  
**Дедлайн:** кінець 9-го тижня  
**Максимум:** 20 балів  
**Бонус:** 5 балів за refresh tokens або OAuth (Google/GitHub)

**Передумова:** виконане ДЗ-1 (Library Management System in-memory) або еквівалентний функціонал.

---

## 🎯 Мета

Розширити backend-застосунок Library Management System:

- **перенести зберігання даних** з оперативної памʼяті в **базу даних**
- реалізувати **реєстрацію та вхід** (аутентифікація)
- захистити API за допомогою **авторизації** (хто має право виконувати дії)
- зберегти REST API та бізнес-логіку з ДЗ-1

---

## 📝 Опис завдання

Ви розширюєте проєкт з ДЗ-1:

1. **База даних** — книги, користувачі та позики зберігаються в БД (SQLite або PostgreSQL). Після перезапуску дані зберігаються.
2. **Аутентифікація** — користувач може зареєструватися та увійти; після входу отримує токен (наприклад, JWT).
3. **Авторизація** — частина ендпоінтів доступна лише авторизованим користувачам; деякі дії (наприклад, видалення книги) — лише адміну.

❗ Ендпоїнти для книг, позик тощо мають працювати як у ДЗ-1, але з читанням/записом у БД та перевіркою прав доступу де потрібно.

---

## 🧱 Технологічні вимоги

- **Node.js**
- **Express**
- **TypeScript**
- **Zod** — валідація запитів
- **База даних:** SQLite (рекомендовано для простоти) або PostgreSQL
- **ORM/запити:** Prisma або Drizzle (або голі SQL-запити через `pg`/`better-sqlite3`)
- **Аутентифікація:** JWT (наприклад, бібліотека `jsonwebtoken`); паролі зберігати **тільки в хешованому вигляді** (bcrypt або argon2)

---

## 📦 Модель даних

Моделі з ДЗ-1 зберігаються в БД. Додатково потрібні поля для аутентифікації та ролей.

### 📘 Book

(Як у ДЗ-1, зберігається в таблиці БД.)

| Поле      | Тип     | Опис                     |
| --------- | ------- | ------------------------ |
| id        | string  | Унікальний ідентифікатор |
| title     | string  | Назва книги              |
| author    | string  | Автор                    |
| year      | number  | Рік видання              |
| isbn      | string  | Унікальний ISBN          |
| available | boolean | Чи доступна книга        |

---

### 👤 User

Розширена модель: додаються поля для входу та ролі.

| Поле         | Тип               | Опис                                           |
| ------------ | ----------------- | ---------------------------------------------- |
| id           | string            | Унікальний ідентифікатор                       |
| name         | string            | Імʼя користувача                               |
| email        | string            | Email (унікальний), використовується для входу |
| passwordHash | string            | Хеш пароля (ніколи не повертати в API)         |
| role         | `USER` \| `ADMIN` | Роль для авторизації                           |

---

### 🔄 Loan

(Як у ДЗ-1, зберігається в таблиці БД з посиланнями на `User` та `Book`.)

| Поле       | Тип                    | Опис                     |
| ---------- | ---------------------- | ------------------------ |
| id         | string                 | Унікальний ідентифікатор |
| userId     | string                 | Користувач               |
| bookId     | string                 | Книга                    |
| loanDate   | Date                   | Дата видачі              |
| returnDate | Date \| null           | Дата повернення          |
| status     | `ACTIVE` \| `RETURNED` | Статус позики            |

---

## 🌐 REST API

### 🔐 Аутентифікація (публічні)

| Метод | Endpoint         | Опис                                |
| ----- | ---------------- | ----------------------------------- |
| POST  | `/auth/register` | Реєстрація (email, пароль, name)    |
| POST  | `/auth/login`    | Вхід (email, пароль) → повертає JWT |

**Приклад тіла для реєстрації/логіну:**

```json
{
  "email": "user@example.com",
  "password": "securePassword123",
  "name": "Іван Петренко"
}
```

(Поле `name` обовʼязкове тільки для `/auth/register`.)

**Відповідь при успішному логіні:**

```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "Іван Петренко",
    "role": "USER"
  }
}
```

---

### 📘 Books

| Метод  | Endpoint     | Опис           | Доступ       |
| ------ | ------------ | -------------- | ------------ |
| GET    | `/books`     | Список книг    | Публічний    |
| GET    | `/books/:id` | Книга за ID    | Публічний    |
| POST   | `/books`     | Створити книгу | Тільки ADMIN |
| PUT    | `/books/:id` | Оновити книгу  | Тільки ADMIN |
| DELETE | `/books/:id` | Видалити книгу | Тільки ADMIN |

---

### 👤 Users

| Метод | Endpoint     | Опис                        | Доступ               |
| ----- | ------------ | --------------------------- | -------------------- |
| GET   | `/users`     | Список користувачів         | Тільки ADMIN         |
| GET   | `/users/:id` | Користувач за ID            | Тільки ADMIN         |
| GET   | `/users/me`  | Поточний користувач (з JWT) | Тільки авторизований |

У відповідях **ніколи** не повертати `passwordHash`.

---

### 🔄 Loans

| Метод | Endpoint            | Опис            | Доступ                                |
| ----- | ------------------- | --------------- | ------------------------------------- |
| POST  | `/loans`            | Видати книгу    | Тільки авторизований (USER/ADMIN)     |
| POST  | `/loans/:id/return` | Повернути книгу | Тільки авторизований                  |
| GET   | `/loans`            | Всі позики      | ADMIN; USER бачить тільки свої позики |

---

## 🔑 Авторизація через JWT

- Клієнт після логіну зберігає токен і надсилає його в заголовку:
  - `Authorization: Bearer <token>`
- Сервер перевіряє токен на захищених маршрутах; при невалідному/відсутньому токені повертає **401 Unauthorized**.
- За потреби перевіряти роль (наприклад, тільки ADMIN) — при відсутності прав повертати **403 Forbidden**.

---

## 📑 Валідація

- Як у ДЗ-1: усі `POST` та `PUT` валідовувати через **Zod**.
- Для `/auth/register` та `/auth/login`: валідувати `email`, `password`; для реєстрації — також `name`.
- Пароль: мінімальна довжина (наприклад, 8 символів); при помилці — **400** з описом.

---

## 📌 Бізнес-логіка

### Реєстрація та логін

- **Реєстрація:** email має бути унікальним; пароль зберігати лише у вигляді хешу.
- **Логін:** перевірити email та пароль (порівняння з хешем); при успіху видати JWT (в payload можна зберігати `userId`, `email`, `role`).

### Книги та позики

- Логіка видачі/повернення книг — як у ДЗ-1 (книга недоступна → не видавати; при поверненні — `available = true`, позика `RETURNED`).
- Всі операції з книгами та позиками виконуються через БД (створення, оновлення, читання).

---

## 📁 Рекомендована структура проєкту

```text
src/
 ├── routes/
 │    ├── auth.routes.ts
 │    ├── books.routes.ts
 │    ├── users.routes.ts
 │    └── loans.routes.ts
 ├── controllers/
 ├── services/
 ├── middleware/
 │    ├── auth.middleware.ts   # перевірка JWT
 │    └── role.middleware.ts  # перевірка ролі (ADMIN)
 ├── schemas/
 ├── db/                       # Prisma/Drizzle схема та клієнт
 ├── app.ts
 └── server.ts
```

---

## ✅ Критерії оцінювання

| Критерій                                               | Бали   |
| ------------------------------------------------------ | ------ |
| Підключення БД, міграції/схема (Book, User, Loan)      | 4      |
| Реєстрація + логін, хешування паролів, JWT             | 4      |
| Middleware перевірки токена та ролі                    | 4      |
| Захист маршрутів (Books/Users/Loans за правилами вище) | 4      |
| Валідація (Zod), коректні коди помилок (400, 401, 403) | 2      |
| Збереження бізнес-логіки позик та цілісність даних     | 2      |
| **Разом**                                              | **20** |

Бонус: refresh tokens або OAuth — **+5** балів.
